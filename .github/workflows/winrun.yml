name: "Windows run attempt"

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: run demo on windows
      run: |
        .\thing.bat
